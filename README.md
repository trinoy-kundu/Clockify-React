🕒 # React Digital Clock

A sleek, real-time digital clock built with React Class Components and Vite. This project demonstrates the core concepts of the React component lifecycle, specifically managing side effects with setInterval.

🚀 ## Features

Real-time Updates: Synchronizes with the system time every second.
Lifecycle Management: Uses componentDidMount to start the timer and componentWillUnmount to clear it, preventing memory leaks.
Dynamic Locales: Supports different time formats via props.
Modern Styling: A clean, dark-mode UI with a digital-inspired font.

🛠️ ## Tech Stack

Framework: React (Class-based)
Build Tool: Vite
Styling: CSS3 (Flexbox & Glassmorphism)
